version: 1
applications:
  - appRoot: apps/web-forwarder
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm install -g pnpm@8.15.4
            - pnpm install --frozen-lockfile
        build:
          commands:
            - cd apps/web-forwarder
            - pnpm run build
      artifacts:
        baseDirectory: apps/web-forwarder/out
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - apps/web-forwarder/node_modules/**/*
          - apps/web-forwarder/.next/cache/**/*
